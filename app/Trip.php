<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Trip extends Model
{
    //Trip(è il nostro model)=>trips(è il nome della tabella(trips_db))
    //
    // $table->id();
    // $table->text('city');
    // $table->text('hotel');
    // $table->integer('days');
    // $table->double('price', 8,2);
    // $table->text('description');
    // $table->tinyInteger(('discount'));
    // $table->timestamps();
}
